[pytest]
addopts = -ra -q --cov=hive_zero_core --cov-report=term-missing --cov-fail-under=0
testpaths = tests
pythonpath = .
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow tests
